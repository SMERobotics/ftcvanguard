[project]
name = "vanguard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TechnoDot", email = "me@technodot.org" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "httpx[socks]>=0.28.1",
    "pydantic-settings>=2.15.0",
    "uvicorn>=0.53.0",
]

[project.scripts]
dev = "vanguard:dev"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
